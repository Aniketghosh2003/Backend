<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-800">
      <div class="form p-20 text-white">
        <form action="/create" method="post">
          <input
            class="block w-full rounded-md outline-none p-5 bg-zinc-500"
            type="text"
            placeholder="Title of the tasks"
            name="title"
          />
          <textarea
            class="block w-full rounded-md outline-none mt-8 p-5 resize-none bg-zinc-500"
            placeholder="Give decription"
            name="details"
          ></textarea>
          <input
            class="mt-12 px-6 py-3 rounded-lg bg-green-600"
            type="submit"
            value="Create task"
          />
        </form>
      </div>
      <div class="tasks flex gap-4 flew-wrap p-20">
        <% if(files.length > 0) { %> <% files.forEach(function (val){ %>
        <div
          class="task w-70 text-white rounded-md px-4 py-3 bg-zinc-500"
        >
          <h1 class="text-2xl tracking-tighter"><%= val %></h1>
          <a class="text-blue-600 inline-block mt-3" href="/files/<%= val %>">Read More</a>
        </div>
        <% }) %> <% } else{ %>
        <h1 class="text-white text-xl">no tasks there</h1>
        <%} %>
      </div>
    </div>
  </body>
</html>
